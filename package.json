{
  "name": "openai-terminal-chatbot",
  "version": "1.0.0",
  "description": "An open AI terminal for commandline",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env src/index.js",
    "dev": "node --env-file=.env --watch src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint src/",
    "clean": "rm -rf node_modules package-lock.json && npm install"
  },
  "keywords": [
    "openai",
    "chatbot",
    "terminal",
    "cli",
    "ai"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20.6.0"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "linebyline": "^1.3.0",
    "openai": "^5.11.0"
  },
  "devDependencies": {
    "eslint": "^9.32.0"
  }
}
